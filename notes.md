# Notas de la Clase 11

## Renderizado de Listas

**Map:**
/\*
// Parte Visual
// callback del map --> Define la función que se va a ejecutar por cada elemento del arreglo.

    array.map((currentItem, indice, array) => {<Element> </Element>})

\*/
